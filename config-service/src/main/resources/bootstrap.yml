server:
  port:8001
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wlorat/one-spring-ms.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
